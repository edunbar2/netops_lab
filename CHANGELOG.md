# Changelog

## Unreleased - Packaging automation

- Added a GitHub Actions packaging workflow for Windows, macOS, Linux, and source zip artifacts.
- Added a PyInstaller-based packaging script that builds the Qt GUI and bundled generator executable.
- Added native packaging outputs for Inno Setup, macOS DMG, and Linux `.deb` where platform tools are available.
- Made packaged Qt builds use a bundled generator executable and per-user settings/output paths.
- Added post-copy macOS app bundle signing and verification so DMGs do not ship with invalid nested binary signatures.
- Reduced packaged build size by avoiding full PySide6 collection and keeping the bundled generator free of Qt/data duplication.
- Added packaging documentation and unit tests for packaging helpers.

## 4.0.1 - GNS3 generation visibility and release guardrails

- Added GitHub pull request, branch, merge, and testing guardrails for patch, minor, and major release work.
- Added a GitHub Actions unit-test workflow for pull requests targeting `main`.
- Changed the Qt command runner to stream generator output while lab generation is running instead of waiting until the command exits.
- Added flushed generation progress stages before and during GNS3 project creation.
- Reused a shared HTTP session for GNS3 API calls to reduce connection overhead during project creation.
- Avoided redundant node-detail API reads when GNS3 create responses already include console metadata.
- Added unit tests for GNS3 API session use, node metadata refresh behavior, and generation progress output.

## 4.0.0 - NetOps Labs foundation

- Renamed the user-facing application to **NetOps Labs**.
- Added the subtitle: "Hands-on infrastructure training for networking, Linux, and security."
- Reworked the primary scenario filter from exam-first selection to **Study Path** selection.
- Added study-path metadata to the bundled catalog while preserving exam alignment as secondary metadata.
- Added backward-compatible study-path derivation for older 3.x scenario metadata.
- Updated generated project metadata schema to 4.0 while retaining existing 3.x lab compatibility.
- Updated package/version metadata to strict `major.minor.patch` versioning.

## 3.0.2

- Reworked macOS and Linux GUI launcher scripts so they reliably launch from the extracted application directory.
- Launchers now prefer a local `.venv`, fall back to system Python, validate PySide6 for the Qt GUI, and provide clearer dependency errors.
- macOS `.command` files now keep the terminal open on launcher errors.
- Unix executable permissions are set for Linux/macOS launcher scripts in the release archive.

## 3.0.0

- Promoted the 3.0 release after RC2 validation.
- Polished the GNS3 Projects filter row so the Show label and dropdown sit together cleanly and no longer crowd the right side of the page.
- Includes the full 3.0 practice workspace, integrated console/lifecycle tooling, server-level GNS3 project management, external-project connection support, generated-project cleanup, and guided-content quality fixes.

## 3.0.0-rc2

- Added a top-level **GNS3 Projects** page for server-level project discovery and cleanup.
- Added project listing from the configured GNS3 server with status, node count, running-node count, project ID, and generated-lab filtering.
- Added server-level actions for selected projects: start nodes, stop nodes, and delete projects with a confirmation dialog that preserves local generated lab folders.
- Added external-project connection support. Existing GNS3 projects that were not generated by the app now open as lightweight workspaces with lifecycle and console support, while clearly noting that guided lab content is unavailable.
- Updated the Qt release banner and application version to 3.0.0-rc2.

## 3.0.0-rc1

- Promoted the 3.0 line to release candidate status.
- Preserved the beta3.3 content/UX fixes: symptom text no longer reveals root cause, scenario IDs are de-emphasized in the Generate table, and structured content rendering has improved spacing.
- Preserved the integrated 3.0 practice stack: guided practice, topology visualization, symbol customization, integrated console access, lifecycle controls, practice notes, and practice summaries.
- Added release-candidate notes for final validation.

## 3.0.0-beta3.3

- Improved learner-facing symptom sanitization so Student Brief / Observed Symptoms do not reveal root-cause clauses such as "because X is missing on Y."
- Kept full root-cause detail in the Answer Key, where the complete explanation belongs.
- Moved scenario IDs out of the first column of the Generate Lab table; titles are now the primary learner-facing identifier and backend IDs appear at the end.
- Reduced answer leakage in the Generate page by removing the prominent Scenario ID line from scenario details.

## 3.0.0-beta3.2

- Improved whitespace and readability for app-rendered structured learning content, not only exported Markdown files.
- Added document-level spacing styles for Qt Markdown panes used by Guided Practice, Documents, Reports, Practice, Lifecycle, and topology details.
- Updated structured JSON document rendering so sections, lists, nested objects, and generated lab content have clearer visual separation in the app.

## 3.0.0-beta3.1

- Suppressed a known harmless Qt `QTextCursor::setPosition: Position '1' out of range` warning that could appear in the terminal while Markdown/text panes were refreshed.
- Other Qt warnings and errors are still printed to stderr.

## 3.0.0-beta3

- Improved guided-lab instructional content quality.
- Reworked generated symptoms to describe user/operator reports rather than naming the exact configuration defect.
- Expanded guided steps with clearer objectives, what-to-look-for guidance, suggested commands, and completion criteria.
- Expanded answer keys into full explanations: root cause, why symptoms occurred, how to prove the issue, corrective action, common wrong fixes, and verification.
- Added additional whitespace between generated document sections for readability.
- Added topic-aware guided templates for BGP, OSPF, EIGRP, STP, EtherChannel, FHRP, switching/VLANs, DHCP relay, policy/ACL/PBR, QoS, and automation/management-plane labs.
- Added `scripts/audit_guided_content.py` for checking generated guided-content structure and readability.

## 3.0.0-beta2.2

- Adjusted the Guided Practice button grid so the second row aligns with the first button column instead of starting under the Learning Mode controls.
- Preserved the beta2.1 guided-practice sizing and JSON document rendering fixes.

## 3.0.0-beta2.1

- Fixed the Guided Practice control row so learning mode and action buttons are readable instead of clipped.
- Changed generated JSON documents in the Documents tab to render as readable Markdown-style sections instead of raw JSON.
- Improved readability for structured sections such as Lab Context and Observed Symptoms.

## 3.0.0-beta2

- Added a Practice tab to the Qt Lab Workspace for the full practice-session workflow.
- Added Start Practice and End Practice actions with per-lab `practice_state.json`.
- Added persistent student notes in `student_notes.md`.
- Added generated `practice_summary.md` when ending a practice session.
- Added quick navigation from Practice to Guided Learning, Topology, Consoles, and Lifecycle.
- Added Guided Practice shortcuts for opening Consoles and Topology.
- Preserved beta1 guided-step workflow, console access, lifecycle controls, topology visualization, and symbol customization.

## 3.0.0-beta1.4

- Bundled the additional GNS3 symbol set under `assets/symbols/`.
- Switched topology defaults to the bundled `classic/` icons for routers, switches, endpoints, firewalls, and clouds.
- Added a Qt Settings dialog for choosing topology symbols by device role.
- Kept fallback drawing for unknown or missing symbols.

## 3.0.0-beta1.3

- Shortened topology link labels so full interface names are easier to read at diagram scale.
- Topology labels now abbreviate interfaces such as `Ethernet0/1` to `e0/1`, `GigabitEthernet2/4` to `g2/4`, and `TenGigabitEthernet1/0` to `te1/0`.
- Full interface names remain available in the topology table and link detail pane.

## 3.0.0-beta1.2

- Bundled the uploaded GNS3 symbols under `assets/symbols/` for application-wide topology visualization use.
- Updated the Qt topology renderer to prefer bundled GNS3 symbols for routers, endpoints, firewalls, and similar device types.
- Added shape-based fallbacks for device types that do not have a matching bundled symbol.
- Improved topology link routing so links attach to node edges instead of running through the center of nodes.
- Offset link labels to reduce overlap with nodes and other labels.

## 3.0.0-beta1.1

- Fixed Guided Practice learning-mode dropdown sizing so Student/Explorer/Instructor text is readable.
- Shortened the Lab Workspace status line so long generated-lab paths do not collide with buttons.
- Removed the Lab Workspace descriptor subtitle to reclaim vertical space.
- Added notes for the planned topology visualization redesign.

## 3.0.0-beta1

- Redesigned the Qt Guided Learning tab into a visible Guided Practice workflow.
- Added explicit step navigation with Previous Step and Next Step controls.
- Added a step list with visible completion status.
- Made Mark Step Complete meaningful by completing the current step and advancing the workflow.
- Added a dedicated hints pane with progressive hint reveal behavior.
- Added Copy Suggested Commands for the current guided step.
- Preserved Student, Explorer, and Instructor learning modes.

## 3.0.0-alpha4.10

- Polished the Generate page right pane after the alpha4 layout redesign.
- Improved the Scenario Details text viewer so it has more reading room and reliable scroll behavior.
- Cleaned up Generation Options field sizing so Project Name and Host Type use sensible widths.
- Fixed vertically cramped Project Name and Host Type controls on macOS.

## 3.0.0-alpha4.9

- Corrected the Generate page right-pane layout so Scenario Details receives the dominant, non-overlapping reading area.
- Constrained Generation Options and action buttons below Scenario Details instead of allowing controls to crowd the scenario text.
- Kept full action button labels while improving the vertical split between scenario reading, controls, and command output.

## 3.0.0-alpha4.7

- Refined the Qt Generate page layout after alpha4.6 feedback.
- Removed the extra Topic/Tag explanatory line above the scenario table to reclaim vertical space.
- Gave Scenario Details higher vertical priority so selected lab information is readable.
- Rebuilt Generation Options as a compact grid to prevent overlap and reduce wasted whitespace.
- Kept full action-button labels while preserving a compact two-column action layout.

## 3.0.0-alpha4.6

- Fixed generated `topology.json` endpoint mapping so topology tables and graphics receive real device/interface/adapter data.
- Changed topology canvas to a neutral `#343434` background with high-contrast links.
- Reworked the Generate page right panel to recover readable scenario details and command output while preserving full button labels.
- Kept the compact two-row filter layout and 3.0 lifecycle/console functionality.

## 3.0.0-alpha4.5

- Polished the Generate tab right-side action buttons so labels no longer crop in the narrower details pane.
- Made the topology visualization background follow the active theme instead of using a fixed dark color.
- Increased topology link contrast and line width, and added rendered-link count to the topology details pane for easier troubleshooting.
- Kept the topology table as the source of truth for exact interface and adapter/port mappings.

## 3.0.0-alpha4.3

- Fixed topology visualization visibility by normalizing topology data and rendering safe `QGraphicsScene.addRect()` device nodes.
- Added fallback topology node derivation from link endpoints so the graphical pane can render even when node metadata is incomplete.
- Reworked Qt theme selection/highlight/header/border styling so custom themes no longer reuse one overbright tertiary color everywhere.
- Improved the Generate page layout for smaller macOS displays by moving filters into a two-row grid, widening the scenario table area, and constraining the scenario detail pane.
- Preserved console connectivity and lifecycle controls from alpha4.x.

## 3.0.0-alpha4.2

- Fixed Qt GUI startup crash caused by missing `QPalette` import in the alpha4.1 theme-highlight patch.
- Preserved alpha4.1 fixes for topology rendering, workspace load recovery, guided-learning initial content, and console endpoint discovery.

## 3.0.0-alpha4.1

- Fixed the Qt topology visualization crash caused by calling `QGraphicsScene.addRoundedRect`; the topology pane now uses the supported `addRoundRect` API.
- Made Lab Workspace loading more fault tolerant so a topology/rendering issue cannot block guided content, reports, lifecycle status, or console endpoint population.
- Restored automatic navigation to the Lab Workspace after successful lab generation by preventing workspace-load exceptions from aborting the post-generation callback.
- Improved Guided Learning initial content so the tab shows useful starting guidance immediately after a lab loads.
- Expanded theme-aware selection/highlight handling through the Qt palette and broader stylesheet selectors, improving light themes and `twilight_parchment`.
- Improved console endpoint discovery by deriving a usable host from the configured GNS3 server when GNS3 metadata omits `console_host`.

## 3.0.0-alpha4

- Added the first Lab Workspace lifecycle controls for deployed GNS3 projects.
- Added lifecycle actions to refresh deployment status, start nodes, stop nodes, reset nodes, and delete the GNS3 project while keeping local generated lab files.
- Added a lifecycle status pane backed by generated lab deployment metadata.
- Preserved alpha1 structured workspace content, alpha2 topology visualization, and alpha3 integrated console groundwork.

## 3.0.0-alpha3

- Added the first integrated console workflow to the Qt Lab Workspace.
- Added a Consoles tab that opens multiple device console sessions from GNS3 console metadata.
- Added basic connect, reconnect, close, and send-command console controls.
- Kept the 3.0 workspace restructuring and topology visualization work in place.
- Added the `twilight_parchment` theme to the Qt GUI, legacy Tk GUI, and setup helper theme list.

# Changelog

## 3.0.0-rc2a

- Widened the GNS3 Projects filter dropdown so all filter labels are visible.
- Added an administrator action to delete all server projects that match the app-generated project filter, with an explicit confirmation dialog.


## 3.0.0-alpha1 - Practice Workspace Foundation

- Begins the 3.0 transition toward an integrated practice application.
- Adds structured per-lab workspace content under `content/*.json`.
- Introduces `lab_manifest.json` schema version 3.0 for scenario labs.
- Slims `student_brief.md` to scenario story, symptoms, context, requirements, expected outcome, and approach.
- Moves project/scenario metadata into `project_info.md` and `content/project_info.json`.
- Adds explicit guided-practice steps in `content/guided_steps.json` and `guided_practice.md`.
- Adds structured hints in `content/hints.json` while keeping `hints.md` as a portable export.
- Adds `topology.json` as groundwork for the future graphical topology view.
- Duplicates configs into `configs/initial/` and `configs/answer_key/` while preserving legacy config folders.
- Updates Qt config browsing so switching between Initial and Answer Key keeps the selected device when possible.

## 2.0.0

- Promoted the PySide6/Qt GUI line to the final 2.0.0 release.
- Kept the legacy Tk/ttkbootstrap GUI as a fallback launcher.
- Preserved the 2.0 release-candidate feature set: Lab Workspace, Guided Learning modes, progressive hints, reports, validation artifacts, topology map table, config viewing, config comparison, advanced tools, and theme-aware selection colors.
- Added final 2.0 release notes.
- Updated version references and documentation for the stable 2.0.0 package.

## 2.0.0

- Stabilization pass for the PySide6/Qt beta line.
- Added persistent per-lab guided learning progress in `.guided_progress.json`.
- Added a Lab Workspace Reports tab for `validation_report.md`, `generation_summary.md`, and `metadata.json`.
- Updated the Qt Settings guidance to reflect final scope.
- Preserved beta2 guided learning modes, config comparison, validation report generation, and installer check/repair commands.

# Changelog

## 2.0.0

- Added Qt Guided Learning tab with Student / Explorer / Instructor modes.
- Added progressive hint reveal and mode-aware answer-key access.
- Added highlighted starter-vs-answer config diff preview.
- Added generated validation_report.md for labs and blank topologies.
- Added metadata/topology/summary/validation output for blank topology generation.
- Added install.py --check and --repair workflows.


## 2.0.0

- Promotes the PySide6/Qt GUI to the default 2.0 beta interface.
- Keeps the legacy Tk/ttkbootstrap GUI packaged as a fallback via `--legacy-tk` and the legacy launchers.
- Consolidates the beta1 parity work into the beta line: Exam filter, Default/Legacy/All deployment selector, learner-facing Topic filter, tag-backed search, generation options, Lab Workspace document/config/topology views, side-by-side config comparison, Advanced Tools, Settings, and persistent local settings.
- Updates installer, launchers, documentation, and version metadata to the beta line.
- Documents remaining 2.0 work: Guided Learning Mode, Student/Instructor/Explorer modes, richer markdown rendering, highlighted config diffs, formal audit report UI, GNS3 Web UI project opening, and installer repair/check workflows.


## 2.0.0

- Fixed Qt GUI startup regression where `normalize_qt_theme` and theme style mappings were missing from the alpha2 file.
- Restored Qt theme normalization and the complete Qt theme style registry so the PySide6 GUI opens cleanly on Windows and other platforms.

## 2.0.0

- Adds a Qt feature-parity pass so the PySide6 GUI exposes more of the mature 1.9.x workflow.
- Adds Qt generation options for project name, host type, node start, Cisco config push, endpoint config push, and verification output.
- Adds a real Qt Settings page with persistent GNS3 server, output directory, theme, host type, push defaults, endpoint credentials, and timeout settings.
- Adds Qt output controls for opening the Lab Workspace, opening the output folder, and clearing command output.
- Adds a Lab Workspace side-by-side config comparison view for starter versus answer-key configs.
- Adds multiple Qt appearance styles, including darker forest/earth themes and softer light themes.
- Adds `docs/QT_PARITY_CHECKLIST.md` to track migrated and deferred GUI features.
- Keeps the Tk GUI packaged as the legacy fallback while Qt moves toward beta quality.

## 1.9.2

- Fixed a 1.9.1 regression where custom dark themes could appear identical to `darkly`.
- Re-applies each custom palette after selecting the isolated theme so `twilight_forest`, `deep_earth`, and `evergreen` remain visually distinct.
- Avoids mutating the built-in `darkly` theme during fallback handling.


## 1.9.1 — Theme isolation bug fix

- Fixed a GUI startup theme isolation bug seen when launching the GUI directly from `install.py`.
- Custom themes now create isolated ttk themes instead of mutating their base Bootswatch theme, so selecting `darkly` after an installer-selected custom theme restores the true built-in darkly appearance without requiring a restart.
- Preserved the 1.9 Lab Workspace, metadata, topology map, hints, and audit tooling from 1.9.0.

## 1.9.0 — Lab confidence and guided learning foundation

- Added generated `topology_map.md` for scenario labs and blank topologies.
- Added generated `metadata.json` so the GUI can load generated lab documents and config sets without guessing folder contents.
- Reworked `hints.md` into a progressive hint file that nudges investigation without immediately revealing the answer.
- Added a GUI **Lab Workspace** tab for reading generated lab docs and viewing starter/answer/baseline configs inside the application.
- Moved blank topology generation into the **Advanced Tools** tab so advanced users keep access without cluttering the guided-learning workflow.
- Added `scripts/audit_labs.py` for offline catalog/topology/interface consistency checks.
- Added GUI **Audit Labs** action.
- Added topology/interface mapping data to generated lab metadata for future guided learning features.

## 1.8.1 — IOL link/interface alignment fix

- Fixed IOL/IOU link creation so catalog logical interface index 0 maps to Ethernet0/0, 1 maps to Ethernet0/1, 2 maps to Ethernet0/2, 3 maps to Ethernet0/3, and 4 maps to Ethernet1/0.
- This fixes labs such as `bgp_wrong_remote_as` where GNS3 links were being created on Ethernet1/0, Ethernet2/0, and Ethernet3/0 while the generated startup config correctly configured Ethernet0/1, Ethernet0/2, and Ethernet0/3.
- Updated live template port preflight logic for IOL/IOU adapter modules instead of treating every logical interface as a separate adapter.
- Added catalog validation for IOL link/interface alignment so future topology/config mismatches are caught earlier.

## 1.8.0 — Reliability and feedback release

- Added CLI `--preflight` readiness checks for the selected scenario.
- Added GUI **Check Readiness** action for selected labs.
- Added generated `generation_summary.md` with project, topology, generated-file, automation, and next-step details.
- Expanded generated student-facing lab content with more verbose mentor-style guidance.
- Improved first-time walkthroughs so they guide investigation without revealing the final answer.
- Preserved detailed answer keys as the place for complete root cause, impact, fix, and verification detail.

## 1.7.3 — GUI startup theme persistence fix

- Fixed GUI startup so the theme selected by `install.py` or `scripts/setup_environment.py` is applied during initial boot.
- Custom themes are now re-applied after widgets are created, so aliases such as `mist`, `sage`, `parchment`, `woodland`, `twilight_forest`, `deep_earth`, and `evergreen` no longer start visually as their base ttkbootstrap theme until manually toggled.
- Invalid saved theme names are normalized back to `darkly` before startup and settings save.

## 1.7.2 — Installer and endpoint push defaults

### Added

- Added root-level `install.py` as the recommended first-run entry point. It can install Python requirements, call the setup wizard, create local config files, discover GNS3 templates, run readiness checks, and optionally launch the GUI.
- Added `--no-push-endpoints` as a clearer CLI alias for disabling endpoint config push; the existing `--skip-endpoints` alias is still supported.

### Changed

- Supported endpoint config push now defaults to enabled in the CLI when not explicitly disabled.
- The GUI now defaults **Push supported endpoints** to enabled for new local settings.
- `config/app_config.example.json` and `scripts/setup_environment.py` now write `push_endpoints: true` for new local installs.
- Endpoint push now implies node start, the same way Cisco config push does.

### Notes

- Users who only want generated endpoint setup files can disable this with `--no-push-endpoints`, `--skip-endpoints`, or by clearing **Push supported endpoints** in the GUI Settings tab.

## 1.7.1 — Lab content guidance release

### Changed

- Reworked generated `student_brief.md` guidance so the first-time walkthrough teaches a troubleshooting process instead of revealing the answer.
- Added generated `first_time_walkthrough.md` for every lab. It provides procedural investigation steps, verification order, and topic-specific diagnostic commands without giving away the fix.
- Expanded generated `answer_key.md` content with root cause, why the issue breaks the lab, corrective action summary, detailed per-fault analysis, verification procedure, expected successful outcomes, and grading criteria.
- Updated learner artifact descriptions so answer configs are clearly treated as a final reference rather than a starting point.

### Notes

- This is a learner-content quality release. Catalog scenario definitions are unchanged; the generator now produces more useful lab-facing documents from the existing metadata.

## 1.7.0 — Setup and usability release

### Added

- Added `scripts/validate_catalog.py` for catalog metadata validation focused on GUI filter quality, required fields, topology references, legacy markers, uncommon values, and one-off tags.
- Added a **Validate Catalog** button in the GUI Settings tab.
- Added direct generated-lab artifact shortcuts in the Output panel: **Open Generated Lab Folder**, **Open Lab README**, and **Open Lab Configs**.

### Changed

- Audited the theme list. Dark themes were kept because they are meaningfully distinct.
- Reduced existing light ttkbootstrap themes to `minty`, `sandstone`, and `morph` because the other light options were mostly minor variations on white backgrounds.
- Added custom softer light themes: `mist`, `sage`, `parchment`, and `woodland`.
- Added darker green/brown themes: `twilight_forest`, `deep_earth`, and `evergreen`.
- Replaced duplicate **Open Last Lab Folder** placements with a single output-area group of generated-lab artifact buttons.
- The lab details action area now focuses on selected-lab actions only: generate, copy CLI command, and copy scenario ID.

### Notes

- `scripts/setup_environment.py` now exposes the curated 1.7 theme list through the existing numbered multi-column selector.
- The existing setup helper remains the recommended first-run configuration path.


## 1.6.3 — Filter cascade and theme expansion

### Fixed

- The **Topic** filter now matches both the catalog `topic` field and broad topical tags such as `routing`, `automation`, `infrastructure`, `security`, `switching`, and related curriculum areas. This prevents broad topic selections from returning almost no labs when the catalog stores the concept as a tag instead of a topic.
- Lab filter dropdowns now cascade from higher-priority selections. For example, after selecting the **Infrastructure** domain, lower-priority topic, tag, difficulty, and lab-type options only show values that still have matching scenarios under Infrastructure.
- Invalid lower-priority filter selections are reset to **All** when a higher-priority selection makes them irrelevant.

### Changed

- Added additional GUI themes to the Settings tab and setup script: `vapor`, `morph`, `journal`, `lumen`, `united`, `cerulean`, `simplex`, `forest`, `earth`, and `moss`.
- Added lightweight custom styling for `forest`, `earth`, and `moss` so the GUI has greener and browner theme options instead of only generic light/dark Bootstrap themes.

## 1.6.2 — GUI sizing and legacy deployment filtering

### Changed

- Increased the default GUI window size to `1760x1280`.
- Increased the minimum GUI window size to `1400x1050` so the helper panel and primary lab buttons remain visible by default.
- Renamed the old **Hide IOSv/IOL variants** control to **Legacy deployments**.
- Changed legacy filtering to use the catalog's `legacy_variant` marker and toggle between default-only and legacy-only views instead of mixing both modes.
- The lab details pane now reports whether the selected scenario is a default or legacy deployment.
- GUI settings now persist the `show_legacy_deployments` preference.

### Catalog review

- No IOSv-backed scenarios are marked as non-legacy in the 1.6.1 catalog baseline. IOSv/IOSvL2 scenarios remain compatibility fallbacks rather than the normal recommended path.


## 1.6.1 — Setup helper and generated-lab folder access

### Added

- `scripts/setup_environment.py`
  - Prompts for GNS3 server URL.
  - Prompts for default endpoint host type.
  - Prompts for output directory and a numbered GUI theme selection.
  - Writes `config/app_config.local.json`.
  - Discovers GNS3 templates.
  - Writes `local_templates_raw.json`.
  - Writes `local_templates_summary.json`.
  - Writes `config/template_overrides.local.json`.
  - Runs a readiness check when possible.
- GUI button: **Open Last Lab Folder**.
- GUI now captures the generator's `Local files:` output and remembers the last generated lab directory.

### Changed

- `docs/DEPLOYMENT_GUIDE.md` now clearly separates:
  - Recommended master setup
  - Manual setup
  - Scripted template-only setup
- `docs/GUI_USER_GUIDE.md` documents generated lab folder access.

### Curriculum

No lab/scenario content changed in 1.6.1.


## 1.6.0 — Deployment and configuration foundation

### Changed

- Removed the personal/default GNS3 server URL from the generator.
- Removed the personal/default GNS3 server URL from the GUI.
- Generator can read `config/app_config.local.json`.
- GUI settings now persist to `config/app_config.local.json`.
- Template overrides can be loaded from `config/template_overrides.local.json`.
- Project generation now requires a server URL from CLI, environment, or local config.

### Added

- `config/app_config.example.json`
- `config/template_overrides.example.json`
- `UPGRADE_NOTICE_1.6.0.md`
- `docs/DEPLOYMENT_GUIDE.md`
- `docs/ENVIRONMENT_READINESS.md`
- `scripts/discover_gns3_templates.py`
- `scripts/generate_template_mapping.py`
- `scripts/print_lab_requirements.py`
- `scripts/check_environment_readiness.py`

### Upgrade notice

Fresh setup is recommended for 1.6.0 because user-specific configuration and template mapping data are now separated from bundled catalog/application files.

### Curriculum

No lab/scenario content changed in 1.6.0.


## 1.5.0 — GUI polish and theme support

### Added

- Theme selector in the Settings tab.
- Theme persistence in `.gns3_lab_gui_settings.json`.
- Beginner-friendly filter for `intro` and `easy` labs.
- Active filter summary in the Labs tab.
- Clear search button.
- Copy CLI command button for selected labs.
- Copy CLI command button for selected study-plan labs.
- Open Output Folder button.
- Generation confirmation summary before creating labs or blank topologies.
- Study-plan table column for estimated minutes.
- Improved lab details formatting.
- Updated `docs/GUI_USER_GUIDE.md`.

### Changed

- Lab difficulty and lab type values are displayed in title case in GUI tables.
- Study-plan workflow messaging is clearer.
- The Settings tab now exposes theme selection alongside generation settings.

### Curriculum

No lab/scenario content changed in 1.5.0.


## 1.4.3 — Study Plans button visibility bugfix

### Changed

- Moved **Generate Selected Plan Lab** and **Generate All Labs in Plan** above the expanding plan details text area.
- Added a scrollbar to the Study Plans details pane.
- Reduced the Study Plans list height slightly so the default GUI layout has more usable vertical space.

### Curriculum

No catalog content changed in 1.4.3.


## 1.4.2 — Template baseline documentation bugfix

### Changed

- Updated bundled catalog template metadata from the current GNS3 `/v2/templates` output.
- Updated IOL2 to 8 Ethernet adapters.
- Updated RHEL9 template ID.
- Documented RHEL9 QEMU requirement: `-cpu host`.
- Documented RHEL9 endpoint login defaults:
  - username: `cloud-user`
  - password: `redhat`

### Added

- `docs/TEMPLATE_BASELINE_1.4.2.md`
- `docs/TEMPLATE_API_CONVERSION_GUIDE.md`

### Curriculum

No lab/scenario content changed in 1.4.2.


## 1.4.1 — GUI sizing bugfix

### Changed

- Increased the default GUI window size.
- Increased the minimum GUI window size to better fit the 1.4 tabbed layout.
- Changed the main content/output area to a resizable vertical pane.
- Added a scrollbar to the output pane.
- Slightly reduced fixed details/table widths to reduce clipping on launch.

### Curriculum

No catalog content changed in 1.4.1.


## 1.4.0 — GUI/UX redesign and catalog tagging

### Added

- Tabbed GUI workflow:
  - Labs
  - Study Plans
  - Blank Topologies
  - Settings
- Searchable lab table with filters for:
  - Exam
  - Domain
  - Topic
  - Tag
  - Difficulty
  - Lab type
- Scenario details preview panel.
- Study-plan table with ordered lab sequence display.
- Blank-topology generation mode in the GUI.
- CLI support for blank topology generation:
  - `--blank-topology --topology <topology_id>`
- Normalized `tags` metadata for scenarios.
- `primary_exam` and `progression_rank` metadata for lab discovery and ordering.
- `docs/GUI_USER_GUIDE.md`
- `docs/CATALOG_TAGGING_GUIDE.md`

### Changed

- Topology selection is no longer a primary part of normal lab selection.
- Topology-only work now lives in the Blank Topologies tab.
- Study plans are ordered by exam domain and progression.
- ENCOR routing-related labs are now discoverable through the normalized `routing` tag.

### Tagging result

```text
ENCOR base scenarios tagged routing: 62
```

### Counts

```text
Templates:                14
Topologies:               54
Unique lab concepts:      213
Scenario entries:         422
Legacy/fallback entries:  209
CCNA unique concepts:     85
ENCOR unique concepts:    158
ENARSI unique concepts:   39
```

### QA result

```text
Issues:    0
Errors:    0
Warnings:  0
```


## 1.3.0 — CCNA completion content wave and exam-tag isolation

### Added

- 24 new unique CCNA foundation/completion labs.
- New CCNA interpretation topology:
  - `ccna_wireless_security_interpretation`
- `docs/EXAM_TAGGING_POLICY.md`
- Updated `docs/CCNA_COVERAGE.md`

### Coverage focus

```text
DHCP
SSH
Syslog
NAT ACL matching
CDP/LLDP
Inter-VLAN routing
BPDU Guard
Port Security
DHCP Snooping
Dynamic ARP Inspection
Wireless security interpretation
REST/CRUD/API interpretation
Ansible inventory basics
Controller API roles
IaC drift
AI/ML operations interpretation
```

### Exam-tag cleanup

- All `ccna_*` scenarios are now CCNA-only.
- All `ccna_*` scenarios now have `primary_exam = "CCNA"`.
- One accidental ENCOR tag was removed from a `ccna_*` IPv6 foundation lab.
- ENCOR unique lab concept count is now 158.

### Counts

```text
Templates:                14
Topologies:               54
Unique lab concepts:      213
Scenario entries:         422
Legacy/fallback entries:  209
CCNA unique concepts:     85
ENCOR unique concepts:    158
ENARSI unique concepts:   39
```

### QA result

```text
Issues:    0
Errors:    0
Warnings:  0
```


## 1.2.0 — CCNA foundation content wave

### Added

- 6 CCNA starter topology families:
  - `ccna_two_router_lan`
  - `ccna_small_switched_lan`
  - `ccna_router_on_a_stick`
  - `ccna_small_ospf`
  - `ccna_services_edge`
  - `ccna_automation_workstation`
- IOSv/IOSvL2 variants for the CCNA starter topologies.
- 30 new unique CCNA foundation labs.
- New study plans:
  - `ccna-core`
  - `ccna-complete`
- `docs/CCNA_COVERAGE.md`

### Coverage focus

```text
Network Fundamentals
Network Access
IP Connectivity
IP Services
Security Fundamentals
Automation and Programmability
```

### Counts

```text
Templates:                14
Topologies:               52
Unique lab concepts:      189
Scenario entries:         374
Legacy/fallback entries:  185
CCNA unique concepts:     61
```

### QA result

```text
Issues:    0
Errors:    0
Warnings:  0
```


## 1.1.0 — Linux endpoint auto-push and richer student briefs

### Added

- Alpine endpoint auto-push through Linux shell console setup.
- RHEL9 endpoint auto-push through Linux shell console setup.
- CLI options:
  - `--linux-endpoint-username`
  - `--linux-endpoint-password`
  - `--linux-endpoint-login-timeout`
- GUI fields:
  - Linux endpoint user
  - Linux endpoint password
- `docs/LINUX_ENDPOINT_AUTO_PUSH.md`

### Changed

- `--push-endpoints` now supports VPCS, Alpine, and RHEL9 endpoint types.
- Generated `student_brief.md` files now include:
  - Scenario story
  - Observed symptoms
  - Requirements
  - Suggested approach
  - Hints
  - Expected results
  - Generated file map
  - First-time or stuck walkthrough
- README and endpoint documentation updated for 1.1.0 behavior.

### Curriculum

No scenario concepts were added in 1.1.0. This is a usability and endpoint automation feature release.

### Counts

```text
Templates:                14
Topologies:               40
Unique lab concepts:      159
Scenario entries:         314
Legacy/fallback entries:  155
```


## 1.0.1 — CLI launcher and endpoint documentation polish

### Added

- `run_cli_linux.sh`
- `run_cli_macos.command`
- `run_cli_windows.bat`

These launchers invoke `gns3_ccnp_lab_generator.py` through Python so CLI flags are not accidentally passed to a GUI launcher, file association, Electron/Chromium process, or editor.

### Changed

- README now shows CLI examples using `python3 ./gns3_ccnp_lab_generator.py` or the new CLI launchers.
- README clarifies that VPCS endpoint auto-push requires `--host-type vpcs`.
- README clarifies that Alpine is commonly used as the default host mode and that Alpine/RHEL setup files are generated but not automatically pushed in 1.0.x.
- `docs/ENDPOINT_AUTO_CONFIG_ROADMAP.md` and `docs/KNOWN_LIMITATIONS.md` were updated with the same endpoint behavior clarification.

### Curriculum

No curriculum content changed in 1.0.1.

### Counts

```text
Templates:                14
Topologies:               40
Unique lab concepts:      159
Scenario entries:         314
Legacy/fallback entries:  155
```


## 1.0.0 — Stable ENCOR curriculum baseline

### Added

- Final usage-focused README.
- `docs/KNOWN_LIMITATIONS.md`.
- Finalized `docs/RELEASE_1.0_SCOPE.md`.
- Finalized `docs/ONE_DOT_ZERO_READINESS.md`.

### Changed

- README is now an introduction and usage guide rather than a release-history document.
- Release history remains in this changelog.
- Version updated to `1.0.0` across the generator, GUI, and `VERSION`.

### Scope

1.0.0 is the first stable baseline for a non-controller ENCOR practical GNS3 curriculum. It includes troubleshooting labs, skill checks, guided study plans, generated learner artifacts, QA metadata, VPCS endpoint auto-configuration, and template preflight checks.

### Counts

```text
Templates:                14
Topologies:               40
Unique lab concepts:      159
Scenario entries:         314
Legacy/fallback entries:  155
```


## 0.18.0 — ENCOR skill-check expansion

### Added

- New `encor-skill-checks` study plan.
- New `skillcheck_encor_eigrp_lab` topology.
- New `skillcheck_encor_eigrp_lab_iosv` topology.
- 8 new unique ENCOR skill-check concepts:
  - IPv6 foundation
  - IPv4/IPv6 dual-stack campus
  - Advanced OSPF multi-area
  - EIGRP advanced routing
  - BGP edge routing
  - QoS trust boundary and WAN policy
  - Security edge policy
  - Automation and assurance workflow
- `docs/ENCOR_SKILL_CHECKS.md`

### Changed

- Existing `skill-checks` study plan now includes expanded ENCOR skill-check groups.
- Existing ENCOR complete study plan now references the expanded skill-check set.

### QA result

```text
Issues:    0
Errors:    0
Warnings:  0
```

### Counts

```text
Templates:                14
Topologies:               40
Unique lab concepts:      159
Scenario entries:         314
Legacy/fallback entries:  155
Skill Check concepts:     12
```


## 0.17.0 — Curriculum QA, answer keys, hints, and expected results

### Added

- `--qa-report` CLI command.
- Standardized scenario QA metadata:
  - `hints`
  - `expected_results`
  - `answer_key`
  - `qa_status`
  - `qa_notes`
  - `learner_artifacts`
- Skill-check `grading_criteria`.
- Generated `grading_criteria.md` artifact.
- `docs/CURRICULUM_QA_REPORT.md`
- `docs/SCENARIO_QA_METADATA.md`

### Changed

- Generated `answer_key.md` now includes root cause, fix summary, fault summary, verification commands, and grading criteria.
- Generated lab artifacts now better support learner self-check and instructor review.

### QA result

```text
Issues:    0
Errors:    0
Warnings:  0
```

### Counts

```text
Templates:                14
Topologies:               38
Unique lab concepts:      151
Scenario entries:         298
Legacy/fallback entries:  147
```


## 0.16.0 — Endpoint auto-configuration and skill-check output polish

### Added

- VPCS endpoint auto-configuration support.
- CLI flags:
  - `--push-endpoints`
  - `--skip-endpoints`
  - `--endpoint-timeout`
- GUI toggle:
  - `Push supported endpoints`
- Endpoint push logs under `endpoint_push_logs/`.
- Generated lab output folders:
  - `starter_configs/`
  - `answer_key_configs/`
  - `endpoint_setup/`
- Generated lab artifacts:
  - `requirements.md`
  - `hints.md`
  - `expected_results.md`
  - `verification.md`
- `docs/RELEASE_1.0_SCOPE.md`

### Changed

- `student_brief.md` now points learners toward starter configs, answer configs, and endpoint setup files.
- Skill-check output is clearer and more suitable for configure-from-requirements labs.

### Counts

```text
Templates:                14
Topologies:               38
Unique lab concepts:      151
Scenario entries:         298
Legacy/fallback entries:  147
```


## 0.15.0 — Skill checks and endpoint auto-config roadmap

### Added

- New `skill-checks` study plan.
- New `skillcheck_enterprise_wan_capstone` topology.
- 4 new unique skill-check concepts:
  - CCNA access edge basic skill check
  - ENCOR campus routing/services skill check
  - ENARSI advanced routing skill check
  - ENCOR enterprise WAN capstone skill check
- `docs/SKILL_CHECKS.md`
- `docs/ENDPOINT_AUTO_CONFIG_ROADMAP.md`

### Notes

Skill checks are configure-from-requirements labs. The clean generated configs represent a possible answer key / target state. The starter/faulty generated configs are intentionally incomplete.

Endpoint setup files are generated today, but endpoint configurations are not generally auto-pushed. The endpoint roadmap recommends VPCS auto-config first, with Linux shell pushes as an opt-in 1.0.0 feature.

### Counts

```text
Templates:                14
Topologies:               38
Unique lab concepts:      151
Scenario entries:         298
Legacy/fallback entries:  147
```


## 0.14.0 — Automation and assurance workflow depth

### Added

- New `automation_assurance_workflow_lab` topology.
- New `automation_assurance_workflow_lab_iosv` legacy topology.
- New `automation_assurance_workstation_setup.j2` host template.
- 10 new unique automation/assurance workflow concepts:
  - Automation inventory script uses wrong JSON key
  - Mock API response contains invalid JSON
  - Down-interface parser logic wrong
  - Event correlator severity case mismatch
  - RESTCONF workflow HTTP server disabled
  - NETCONF blocked by VTY transport
  - Syslog severity too restrictive for workflow
  - SNMP community mismatch in workflow
  - NetFlow summary uses wrong field name
  - Syslog correlation uses wrong device

### Counts

```text
Templates:                14
Topologies:               37
Unique lab concepts:      147
Scenario entries:         291
Legacy/fallback entries:  144
```


## 0.13.0 — Security policy depth

### Added

- New `security_policy_depth_lab` topology.
- New `security_policy_depth_lab_iosv` legacy topology.
- 10 new unique security depth concepts:
  - ASAv NAT rule shadowed by broad dynamic NAT
  - ASAv twice NAT destination object wrong
  - ASAv outside ACL uses real instead of mapped IP
  - ASAv access group on wrong interface
  - ASAv ICMP inspection missing
  - ASAv SSH management allowed on wrong interface
  - IOS ACL broad permit shadows policy
  - IOS strict uRPF breaks asymmetric path
  - IOS VTY ACL too broad
  - IOS ACL missing explicit deny log

### Counts

```text
Templates:                14
Topologies:               35
Unique lab concepts:      137
Scenario entries:         271
Legacy/fallback entries:  134
```


## 0.12.0 — Advanced routing + GUI study plans

### Added

- GUI Guided Study Plan section.
- Study-plan selector in GUI.
- Study-plan lab selector in GUI.
- New `advanced_routing_lab` topology.
- New `advanced_routing_lab_iosv` legacy topology.
- 10 new unique advanced routing concepts:
  - Redistribution missing subnets
  - Redistribution route map denies routes
  - Prefix list too specific
  - OSPF summary mask wrong
  - OSPF default originate missing
  - BGP missing OSPF redistribution
  - BGP network mask wrong
  - Floating static administrative distance wrong
  - OSPF distribute list blocks route install
  - OSPF external metric type wrong

### Counts

```text
Templates:                14
Topologies:               33
Unique lab concepts:      127
Scenario entries:         251
Legacy/fallback entries:  124
```


## 0.11.0 — Curriculum structure and 1.0.0 readiness

### Added

- `--coverage` CLI command.
- `--list-study-plan` CLI command.
- `encor-core` guided study plan.
- `encor-complete` guided study plan.
- Scenario curriculum metadata:
  - `curriculum_track`
  - `domain_order`
  - `difficulty_order`
  - `prerequisites`
  - `skills`
- `docs/ENCOR_COVERAGE_MATRIX.md`
- `docs/GUIDED_STUDY_PLANS.md`
- `docs/ONE_DOT_ZERO_READINESS.md`

### Changed

- No new lab concepts were added. This release is a curriculum organization and readiness release.

### Counts

```text
Templates:                14
Topologies:               31
Unique lab concepts:      117
Scenario entries:         231
Legacy/fallback entries:  114
```


## 0.10.0 — Access edge services/security wave

### Added

- New `access_edge_services_lab` topology.
- New `access_edge_services_lab_iosv` legacy topology.
- 10 new unique lab concepts:
  - DHCP snooping uplink not trusted
  - DHCP snooping VLAN missing
  - DAI trust missing on uplink
  - Port security maximum too low
  - Port security violation mode shutdown
  - BPDU Guard missing on access port
  - Storm control missing
  - Errdisable recovery missing
  - HSRP tracks wrong interface
  - HSRP decrement too low

### Changed

- GUI default size increased to `1600x1000`.
- GUI minimum size increased to `1280x850`.
- Output pane height/padding adjusted so it is less likely to be clipped at the bottom.

### Counts

```text
Templates:                14
Topologies:               31
Unique lab concepts:      117
Scenario entries:         231
Legacy/fallback entries:  114
```


## 0.9.0 — Template metadata refresh and semantic versioning

This release corresponds to the historical `v29` package.

### Changed

- Retired the old `vNN` release naming.
- Adopted `major.minor.patch` versioning.
- Added a `VERSION` file.
- Added `--version` to `gns3_ccnp_lab_generator.py`.
- Updated the GUI title/header to show the current version.
- Refreshed compact catalog template metadata from the latest GNS3 `/v2/templates` export.
- Updated IOL2 template metadata:
  - `template_id`: `5b3b5780-0b02-4ed4-a8ac-ad5cd269f497`
  - `ethernet_adapters`: `4`
  - `serial_adapters`: `0`

### Counts

```text
Templates:                14
Topologies:               29
Unique lab concepts:      107
Scenario entries:         211
Legacy/fallback entries:  104
```

### Versioning rules

```text
0.x.y  pre-feature-complete releases
x.y.0  content/feature/schema/tooling releases
x.y.z  bugfix-only releases
1.0.0  feature-complete ENCOR curriculum/application baseline
```


## 2.0.0

- Restored the Qt Exam filter and placed it before Deployment mode in the primary scenario filter bar.
- Kept the Default/Legacy/All deployment selector, but demoted it behind Exam because exam scope is usually the learner's first filter.
- Simplified Topic vs Tag UX: Topic remains the curated learner-facing dropdown; raw tags remain searchable but no longer occupy a top-level Qt dropdown.
- Added exam values to the Qt scenario table and search haystack.
