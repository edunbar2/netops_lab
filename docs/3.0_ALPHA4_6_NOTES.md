# 3.0.0-alpha4.6 Notes

This is a focused UI/data fix for the 3.0 alpha4 line.

## Fixed

- Corrected `topology.json` generation so device names, interfaces, and GNS3 adapter/port values are populated from the normalized topology rows.
- Changed the topology canvas to a neutral `#343434` background and high-contrast link lines.
- Reworked the Generate page right-side panel so scenario details, generation options, buttons, and command output are readable again.

## Note

Existing labs generated by alpha4.5 may still contain bad `topology.json` endpoint data. Regenerate the lab with alpha4.6 to get corrected topology data.
