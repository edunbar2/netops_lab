# 3.0.0-beta2.1 Notes

This patch fixes two beta2 UI issues.

## Fixes

- Guided Practice buttons and the Learning Mode dropdown now have enough room to display their labels.
- Structured JSON-backed documents, including Lab Context, render in the Documents tab as readable Markdown-style content instead of raw JSON.
